# Config-modules

Wrapper modules for flake inputs used in NixOS configurations.

Each sub-directory wraps an external flake module (stylix, lanzaboote, flatpak, nix-index-db) with the project's option conventions.

Imported by `configuration.nix` automatically.
